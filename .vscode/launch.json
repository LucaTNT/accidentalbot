{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Accidentalbot",
      "program": "${workspaceFolder}/accidentalbot.js",
      "request": "launch",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "envFile": "${workspaceFolder}/.env"
    },
    {
      "name": "Docker Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node"
    }
  ]
}